name: Pushed
on:
  push:
    paths:
      - 'plugins/*'

jobs:
  build:
    name: Push job
    runs-on: ubuntu-latest
    steps: 
      - uses: actions/checkout@v1
      - name: Upsert File
        env:
          GITHUB_TOKEN: ${{ secrets.MY_TOKEN }}
        uses: ./.github/actions/build
        id: upsertFile